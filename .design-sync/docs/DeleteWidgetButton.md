---
category: Widgets
---
Destructive `ลบวิดเจ็ต` button with a built-in confirmation `AlertDialog`; `onDelete` fires only after the user confirms.

## Usage

```tsx
import { DeleteWidgetButton } from "trailblazer-ui";

<DeleteWidgetButton onDelete={() => {}} isLoading={false} />
```
