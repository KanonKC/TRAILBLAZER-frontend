---
category: Widgets
---
Enable step control: a small `▶ เปิดใช้งาน` button, or the green `เปิดใช้งานแล้ว` pill once `isEnabled`.

## Usage

```tsx
import { WidgetStatusControl } from "trailblazer-ui";

<WidgetStatusControl isEnabled={false} isSaving={false} onEnable={() => {}} />
```
