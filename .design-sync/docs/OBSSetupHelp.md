---
category: Widgets
---
Collapsible `วิธีตั้งค่าใน OBS` instructions; `type="audio"` adds the audio-monitoring steps. `variant="overlay"` (default) is translucent, `"default"` is a card.

## Usage

```tsx
import { OBSSetupHelp } from "trailblazer-ui";

<OBSSetupHelp type="audio" defaultOpen />
```
