---
category: Widgets
---
The save + test button pair at the bottom of a widget form (`isSaving`, `isTesting`, `canTest`).

## Usage

```tsx
import { WidgetTestControl } from "trailblazer-ui";

<WidgetTestControl isSaving={false} isTesting={false} canTest onSave={() => {}} onTest={() => {}} />
```
