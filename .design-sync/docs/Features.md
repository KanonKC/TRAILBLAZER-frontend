---
category: Landing
---
Landing-page feature grid: heading with a gradient word, then six `glass` cards (icon tile in the orange gradient, title, description). Self-contained - no props.

## Usage

```tsx
import { Features } from "trailblazer-ui";

<Features />
```
