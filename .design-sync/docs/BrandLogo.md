---
category: Brand
---
The wordmark: light-weight Kanit `TRAIL` in white + `BLAZER` in the orange-gold gradient (`trailblazer-gradient-text`). Inline `<span>` - size it with a surrounding `text-2xl`/`text-4xl` class.

## Usage

```tsx
import { BrandLogo } from "trailblazer-ui";

<h1 className="text-4xl"><BrandLogo /></h1>
```
