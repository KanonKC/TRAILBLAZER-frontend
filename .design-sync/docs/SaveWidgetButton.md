---
category: Widgets
---
Primary save button for a widget settings form: shows `กำลังบันทึก...` and disables while `isLoading`.

## Usage

```tsx
import { SaveWidgetButton } from "trailblazer-ui";

<SaveWidgetButton onSave={() => {}} isLoading={false} />
```
